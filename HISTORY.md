# 플러그인게시판(plugin_board) 변경 기록

버전별 제목은 `## 버전 - 날짜` 형식을 씁니다. 플러그인게시판은 설치된 버전 이후의
구간만 잘라서 "업데이트 변경 내용"으로 보여줍니다.

## 2.51.2 - 2026-09-23

### 수정
- Gitea 서버에도 2.51.1의 토픽 검색 수정을 적용.
  - 토픽 검색·소유자 스캔을 `X-Total-Count` 기준으로 끝까지 페이지 조회(Gitea는 limit과 무관하게
    보통 50개씩만 돌려줘서 첫 페이지 이후 결과가 빠졌음).
  - 토픽 결과를 설정 순서(카탈로그 → 추가 발견 → 기본)대로 병합, 서버당 상한 200개.
- 같은 이름의 저장소가 GitHub와 Gitea에 모두 있을 때 Gitea 카드가 조용히 버려지던 문제 — 이 서버가
  Gitea 쪽에서 설치한 플러그인이면 Gitea 카드를 표시.

### 추가
- Gitea 연결 테스트 결과에 검색한 토픽 목록 표시.

## 2.51.1 - 2026-09-23

### 수정
- 추가 발견 토픽·카탈로그 토픽 결과가 목록에서 빠지던 문제. 기본 토픽 결과만으로 30개 상한을 넘어,
  병렬 도착 순서에 따라 나머지 토픽 결과가 잘리고 있었음. 상한을 200개로 올리고(VERSION 파일이
  확인된 저장소만 카드가 되므로 안전), 결과 순서를 카탈로그 → 추가 발견 → 기본 토픽 순으로 고정.
- GitHub 토픽 검색을 토픽당 최대 300개(100개 × 3페이지)까지 조회.

### 추가
- 헤더의 토픽 검색 시각 옆에 실제로 검색한 토픽 목록 표시.

## 2.51.0 - 2026-09-23

### 추가
- Gitea 소유자 단위 발견: 설정의 Gitea 서버에 "소유자"(사용자·조직, 쉼표 구분)를 적으면 그 소유자의
  저장소를 토픽 없이도 전부 살펴보고, VERSION 파일(`plugin version`)이 있는 저장소를 플러그인
  카드로 표시(저장된 계정으로 비공개 저장소 포함). 이 서버에서 설치한 적 있는 소유자는 자동 포함.
- 연결 테스트에 소유자별 결과(저장소 수, 비공개 수, 플러그인으로 인식된 수) 추가.
- 설치 칸에 소유자 주소(`https://서버/소유자/`)를 넣으면 소유자 등록 방법을 안내.

### 변경
- 발견 카드 30개 상한을 GitHub 토픽 결과에만 적용하고, Gitea는 서버당 100개로 분리.
- Gitea 저장소의 VERSION 조회 결과를 1시간 캐시(소유자 스캔 시 매번 재조회하지 않음).

## 2.50.0 - 2026-09-23

### 변경
- 설정의 "Gitea 서버"를 서버별 주소(스킴·포트 포함) / 아이디 / 비밀번호 / 읽기 토큰으로 저장
  (JSON 목록). 구버전 `호스트:토큰` 문자열도 계속 읽으며, 화면에서 한 번 저장하면 새 형식으로 바뀜.
- 자격증명 없는 Gitea 주소(`https://서버/소유자/저장소`)로 설치하거나 주소를 변경하면, 저장된
  계정으로 자동 인증하고 `https://아이디:비밀번호@서버/...` 형태로 등록. API 호출은 읽기 토큰 우선.
- 설정에서 비밀번호를 바꾸면 자동 등록된 주소도 다음 목록 조회 때 함께 갱신.
- 토픽 검색·버전 확인이 서버별 스킴(http/https)을 따르고, 인증 정보가 바뀌면 검색 캐시를 새로 조회.

### 추가
- Gitea 서버별 "연결 테스트": 서버 접속, 읽기 토큰, 아이디/비밀번호, 토픽 검색(인증 전후 비교)을
  단계별로 진단.
- 설치 이력이 있는 Gitea 서버를 인증 없이 검색해 결과가 0개면 "비공개 저장소가 검색되지 않았을
  수 있음" 안내 카드 표시(관리자 전용).

### 수정
- `git.example.com:3000:토큰`처럼 포트가 있는 호스트를 아이디/비밀번호로 잘못 해석하던 문제.
- 검색 실패 카드에 "신규설치" 버튼이 표시되던 문제.

## 2.49.0 - 2026-09-23

### 추가
- 플러그인별 이력 보기: 카드의 "이력" 버튼과 헤더의 "설치 이력" 버튼(관리자 전용).
- 이 서버 기록: 설치·업데이트·롤백·실패·삭제·활성화 변경, 버전 전후, 출처,
  수동/자동, 실행 관리자, 변경 파일 요약을 `plugins/data/plugin_board/history.jsonl`에 기록.
- 변경 내용: 저장소의 `HISTORY.md` / `CHANGELOG.md` / `CHANGES.md`에서 설치 버전 이후
  구간을 보여주고, 파일이 없으면 GitHub/Gitea Releases로 폴백.
- 자기 자신의 `HISTORY.md` 추가 및 update_manifest 파일 목록에 포함.

## 2.48.0 - 2026-09-23

### 보안
- 관리자 판별을 fail-closed로 변경(세션 role을 확인할 수 없으면 비관리자).
- 카드 액션을 문서화된 범용 RPC(`run_context_menu_action`)로 이전, 구버전 코어는 자동 폴백.
- 설치 버전 배지·설정 라벨의 innerHTML 삽입 제거, `github.txt` 권한 600, 7z 심볼릭 링크 거부.

### 변경
- 설치 폴더명과 클래스 id를 분리. 활성화·설정 키와 로드 검증은 클래스 id 기준.
  `네임스페이스.이름` 형식 id 지원.
- 설치 시점 id 충돌 감지, 스테이징 → 백업 → 교체 → 로드 검증 → 실패 시 복원.
- 정적 검증을 가이드 §2-5에 맞춤(os.system/popen/exec*/spawn*, `platform.system()` 오탐 제거),
  권장 필드는 경고로 완화, `detail_view` 번들 검사 추가.
- 분류를 홈 화면 위젯 / 플러그인 데스크 위젯 / 도서 상세 확장으로 구분.
- 캐시를 Redis(코어 플러그인 캐시)로 워커 간 공유, 디스크 캐시는 `plugins/data/plugin_board/`로 이동.

### 수정
- 비관리자에게 "캐시 초기화" 버튼이 보이던 문제(`hidden` 속성이 CSS에 덮이던 문제).
- 설정 저장 시 `textarea` 값 누락.
